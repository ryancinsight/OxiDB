// src/api/traits.rs
//! Defines abstract patterns of behavior (traits) for the API layer.

// Example (to be expanded later):
// pub trait ApiRequestHandler {
//     type Request;
//     type Response;
//     fn handle(&self, request: Self::Request) -> Result<Self::Response, ApiError>;
// }
